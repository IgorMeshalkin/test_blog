import React from 'react';
import '../../styles/global.css'

const Header = () => {
    return (
        <div className="w-full h-full flex items-center justify-center text-text text-2xl font-bold">
            <span>Test Blog App</span>
        </div>
    );
};

export default Header;